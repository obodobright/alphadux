import styled from "styled-components";
import { colors } from "../colors";

export const Spacer = styled.div`
  margin: 1rem 0.2rem;
  width: 100%;
  color: red;
  border-bottom: 1px solid ${colors.profile_bg};
`;
